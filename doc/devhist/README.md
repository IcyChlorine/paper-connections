# Development History

This folder contains development-history records that support future work without mixing historical session notes into the main `doc/` reference set.

- `session-*.md`: chronological session summaries, including worktree-specific files such as `session-2026-03-07awt2-summary.md`.
- Keep current product, storage, and workflow references in the `doc/` root.
- Put new session summaries in this folder, not in the `doc/` root.
